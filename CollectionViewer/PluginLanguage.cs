namespace CollectionViewer;

/// <summary>UI languages supported by the plugin. English is the default so the plugin is usable
/// out of the box for non-Russian-speaking users; Russian is offered as an explicit opt-in.</summary>
public enum PluginLanguage
{
    English,
    Russian,
}
